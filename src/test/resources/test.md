**Exploration: stream
**Found 40 symbols across 4 files.
**Source Code**> The code below is the
 **verbatim, current on-disk source
 ** of these files — re-read from disk on this call and line-numbered, byte-for-byte identical to what the Read tool returns.
 **ValidatorResult.java** — com.haizhi.stream.validator, ValidatorResult, buffer, errorRows, success — 
 `/Users/wugao-pc/Desktop/Project/stream/src/main/java/com/haizhi/stream/validator/ValidatorResult.java````java1	package com.haizhi.stream.validator;2	3	4	import java.util.ArrayList;5	import java.util.List;6	import java.util.Map;7	8	import com.google.common.collect.Maps;9	import com.graph.atlas.common.base.constant.Constants;10	import com.haizhi.stream.task.GLogBuffer;11	import lombok.Data;12	import lombok.EqualsAndHashCode;13	import lombok.NoArgsConstructor;14	15	16	17	/**18	 * Created by chengmo on 2019/6/17.19	 */20	@Data21	@NoArgsConstructor22	@EqualsAndHashCode()23	public class ValidatorResult {24	    private GLogBuffer buffer;25	    private List<Map<String, Object>> errorRows;26	    private boolean success;27	28	    @SuppressWarnings("MagicNumber")29	    public ValidatorResult(int logBufferSize) {30	        this.buffer = new GLogBuffer(logBufferSize > 0 ? logBufferSize : 100);31	        this.errorRows = new ArrayList<>(Constants.LIST_INIT_CAPACITY);32	    }33	34	    public boolean hasErrorRow() {35	        return !this.errorRows.isEmpty();36	    }37	38	    public boolean hasErrorMsg() {39	        return this.buffer.size() > 0;40	    }41	42	    public void addErrorMsg(String message) {43	        this.buffer.addLine(message);44	    }45	46	    public void addErrorRow(Map<String, Object> row) {47	        this.errorRows.add(Maps.newHashMap(row));48	    }49	50	    public void addErrorRows(List<Map<String, Object>> rows) {51	        if (rows == null) {52	            return;53	        }54	        this.errorRows.addAll(rows);55	    }56	57	    public List<Object> getAllErrorMsg() {58	        return this.buffer.getLines();59	    }60	}```**ApiInboundValidator.java** — clustered — `
 /Users/wugao-pc/Desktop/Project/stream/src/main/java/com/haizhi/stream/validator/ApiInboundValidator.java````java45	    @Override46	    protected boolean doValidate(DcInboundDataSuo cuo, ValidatorResult result) {47	        String graph = cuo.getGraph();48	        if (StringUtils.isBlank(graph)) {49	            result.addErrorMsg(new ErrorMsg(ValidateType.GRAPH_BLANK).toString());50	            result.setErrorRows(cuo.getRows());51	            return false;52	        }53	        String schema = cuo.getSchema();54	        if (StringUtils.isBlank(schema)) {55	            result.addErrorMsg(new ErrorMsg(ValidateType.SCHEMA_BLANK).toString());56	            result.setErrorRows(cuo.getRows());57	            return false;58	        }59	        Domain domain= dcMetadataBaseCache.getDomain( graph);60	        if (domain.invalid()) {61	            result.addErrorMsg(new ErrorMsg(ValidateType.GRAPH_NOT_FOUND, graph).toString());62	            result.setErrorRows(cuo.getRows());63	            return false;64	        }65	        Schema sch= domain.getSchema(cuo.getSchema());66	        if (sch == null) {67	            dcMetadataBaseCache.refresh(graph);68	            sch = domain.getSchema(schema);69	        }70	        if (Objects.isNull(sch)) {71	            result.addErrorMsg(new ErrorMsg(ValidateType.SCHEMA_NOT_FOUND, schema).toString());72	            result.setErrorRows(cuo.getRows());73	            return false;74	        }75	        return checkRows(sch, cuo, result);76	    }77	78	    ///////////////////////79	    // private functions80	    ///////////////////////81	    private boolean checkRows(Schema sch, DcInboundDataSuo cuo, ValidatorResult result) {82	        List<Map<String, Object>> rows = cuo.getRows();83	84	        boolean isEdge = sch.getType().isEdge();85	        if (CollectionUtils.isEmpty(rows)) {86	            result.addErrorMsg(new ErrorMsg(ValidateType.DATA_ROWS_EMPTY).toString());87	            return false;88	        }89	        for (Iterator<Map<String, Object>> iterator = rows.iterator(); iterator.hasNext(); ) {90	            Map<String, Object> row = iterator.next();91	            if (!SchemaType.EVENT_DETAIL.equals(sch.getType()) && !checkExternalKeys(row, isEdge, result)) {92	                result.addErrorRow(row);93	                iterator.remove();94	                continue;95	            }96	            Map<String, Object> rowCopy = Maps.newHashMap(row);97	            if (!checkRow(row, sch, isEdge, result, cuo)) {98	                result.addErrorRow(rowCopy);99	                iterator.remove();100	            }101	102	        }103	        // all rows validate failedWithFinish104	        if (rows.isEmpty()) {105	            Loggers.debug(() -> "rows is empty");106	            return false;107	        }108	        return true;109	    }110	111	    private boolean checkRow(Map<String, Object> row, Schema sch, boolean isEdge, ValidatorResult result, DcInboundDataSuo cuo) {112	        boolean success = true;113	        List<Map<String, Object>> arangoRows = cuo.getArangoRows();114	        Map<String, Object> arangoRow = new HashMap<>();115	        String objectKey = Getter.get(Keys.OBJECT_KEY, row);116	        for (Iterator<Map.Entry<String, Object>> iterator = row.entrySet().iterator(); iterator.hasNext(); ) {117	118	            Map.Entry<String, Object> entry = iterator.next();119	            String field = entry.getKey();120	            Object value = entry.getValue();121	            if (StringUtils.equalsIgnoreCase(Keys.ID, field)) {122	                iterator.remove();123	                continue;124	            }125	            SchemaField schemaField = sch.getField(field);126	            if (Objects.isNull(schemaField)) {127	                result.addErrorMsg(new ErrorMsg(ValidateType.MISS_SCHEMA_FIELD, field).toString());128	                iterator.remove();129	                continue;130	            }131	132	            if (!checkDataType(row, schemaField, field, value)) {133	                // if schema field is main,must check data type,otherwise ,will remove this134	                // value135	                if (schemaField.isMain()) {136	                    success = false;137	                    result.addErrorMsg(new ErrorMsg(ValidateType.INVALID_DATA_TYPE_MAIN, field, schemaField.getType(),138	                            value, objectKey).toString());139	                } else {140	                    iterator.remove();141	                    result.addErrorMsg(142	                            new ErrorMsg(ValidateType.INVALID_DATA_TYPE, field, schemaField.getType(), value, objectKey)143	                                    .toString());144	                }145	            }146	            if (success) {147	                arangoRow.put(field, row.get(field));148	            }149	        }150	151	        success = isSuccess(row, sch, isEdge, success, result);152	        if (success) {153	            arangoRows.add(arangoRow);154	        }155	        return success;156	    }157	158	    private boolean isSuccess(Map<String, Object> row, Schema sch, boolean isEdge, boolean success,159	                              ValidatorResult result) {160	        if (!checkAllMainSchemaField(sch, row, result)) {161	            return false;162	        }163	        int size = row.size();164	        // if vertex: key = object_key165	        if (!isEdge) {166	            return success;167	        }168	        // if edge: key = object_key,from_key,to_key169	        if (size < IS_EDGE_NEED_SIZE) {170	          result.addErrorMsg(new ErrorMsg(ValidateType.EDGE_MISS_MUST_FIELD).toString());171	            return false;172	        }173	        return success;174	    }175	176	    private boolean checkAllMainSchemaField(Schema sch, Map<String, Object> row, ValidatorResult result) {177	        boolean check = true;178	        Map<String, SchemaField> fieldMap = sch.getFieldMap();179	        if (Objects.nonNull(fieldMap)) {180	            Set<String> fields = row.keySet();181	            for (Map.Entry<String, SchemaField> fieldEntry : fieldMap.entrySet()) {182	                if (!fields.contains(fieldEntry.getKey())) {183	                    if (fieldEntry.getValue().isMain()) {184	                        check = false;185	                        result.addErrorMsg(186	                                new ErrorMsg(ValidateType.DELETION_MAIN_FIELD_VALUE, fieldEntry.getKey()).toString());187	                    } else {188	                        result.addErrorMsg(189	                               new ErrorMsg(ValidateType.DELETION_FIELD_VALUE, fieldEntry.getKey()).toString());190	                    }191	                }192	            }193	        }194	        return check;195	    }196	197	    @SuppressWarnings("MethodLength")198	    private boolean checkExternalKeys(Map<String, Object> row,199	                                      boolean isEdge,200	                                      ValidatorResult result) {201	        if (!row.containsKey(Keys.OBJECT_KEY)) {202	            result.addErrorMsg(new ErrorMsg(ValidateType.MISS_OBJECT_KEY).toString());203	            return false;204	        }205	        Object objectKey = row.get(Keys.OBJECT_KEY);206	        if (objectKey instanceof String) {207	            String keyString = (String) objectKey;208	            if (keyString.contains("/")) {209	                result.addErrorMsg(new ErrorMsg(ValidateType.ILLEGAL_OBJECT_KEY, objectKey).toString());210	                return false;211	            }212	        }213	        if (isEdge) {214	            Object fromKey = row.get(Keys.FROM_KEY);215	            if (!row.containsKey(Keys.FROM_KEY)) {216	                result.addErrorMsg(new ErrorMsg(ValidateType.MISS_FROM_KEY, fromKey).toString());217	                return false;218	            }219	220	            if (graphConfig.isEdgeConfig()) {221	                Object fromType = row.get(Keys.FROM_TYPE);222	                if (!row.containsKey(Keys.FROM_TYPE)) {223	                    result.addErrorMsg(new ErrorMsg(ValidateType.MISS_FROM_TYPE, fromType).toString());224	                    return false;225	                }226	227	                Object toType = row.get(Keys.TO_TYPE);228	                if (!row.containsKey(Keys.TO_TYPE)) {229	                    result.addErrorMsg(new ErrorMsg(ValidateType.MISS_TO_TYPE, toType).toString());230	                    return false;231	                }232	            }233	234	            if (!graphConfig.isEdgeConfig() && fromKey instanceof String) {235	                String keyString = (String) fromKey;236	                if (!keyString.contains("/")) {237	                    result.addErrorMsg(new ErrorMsg(ValidateType.ILLEGAL_FROM_KEY, fromKey).toString());238	                    return false;239	                }240	            }241	242	            Object toKey = row.get(Keys.TO_KEY);243	            if (!row.containsKey(Keys.TO_KEY)) {244	                result.addErrorMsg(new ErrorMsg(ValidateType.MISS_TO_KEY, toKey).toString());245	                return false;246	            }247	            if (!graphConfig.isEdgeConfig() && toKey instanceof String) {248	                String keyString = (String) toKey;249	                if (!keyString.contains("/")) {250	                    result.addErrorMsg(new ErrorMsg(ValidateType.ILLEGAL_TO_KEY, toKey).toString());251	                    return false;252	                }253	            }254	255	        }256	        return true;257	    }258	259	    @SuppressWarnings("MethodLength")260	    private boolean checkDataType(Map<String, Object> row, SchemaField schemaField, String key, Object value) {261	        try {262	            String fieldValue = Objects.toString(value, "");263	            Object parsedValue = null;264	            switch (schemaField.getType()) {265	                case STRING:266	                    if (Objects.isNull(value)) {267	                        fieldValue = null;268	                    } else if (value instanceof Date) {269	                        parsedValue = com.graph.atlas.common.base.util.DateUtils.formatLocal((Date) value);270	                    }271	                    break;272	                case LONG:273	                    if (value instanceof Long) {274	                        parsedValue = value;275	                    } else if (value instanceof Double) {276	                        // 浮点型转长整型277	                        parsedValue = ((Double) value).longValue();278	                    } else if (value instanceof Date) {279	                        // 日期转时间戳280	                        parsedValue = ((Date) value).getTime();281	                    } else {282	                        // 适配 "23.22" 这样的小数字符串283	                        parsedValue = Double.valueOf(fieldValue).longValue();284	                    }285	                    break;286	                case DOUBLE:287	                    if (value instanceof Double) {288	                        parsedValue = value;289	                    } else if (value instanceof Long) {290	                        // 长整型转浮点型291	                        parsedValue = ((Long) value).doubleValue();292	                    } else {293	                        parsedValue = Double.valueOf(fieldValue);294	                    }295	                    break;296	                case DATETIME:297	                case DATE:298	                    if (value instanceof Long) {299	                        try {300	                            // 时间戳转日期301	                            parsedValue = new Date((Long) value);302	                        } catch (Exception e) {303	                            // 不是有效的时间戳304	                            return false;305	                        }306	                    } else if (value instanceof Date) {307	                        parsedValue = value;308	                    } else if (!DateUtils.isDate(fieldValue)) {309	                        return false;310	                    } else if (FieldVersionEnum.VERSION_2.getValue().equals(schemaField.getVersion())) {311	                        try {312	                            Date fieldDate = DateUtils.parseDate(fieldValue);313	                            parsedValue = fieldDate == null ? null : fieldDate.getTime();314	                        } catch (ParseException e) {315	                            log.warn("时间格式错误，字段:{},值:{}", schemaField.getSchemaName(), fieldValue);316	                            return false;317	                        }318	                    } else {319	                        parsedValue = fieldValue;320	                    }321	                    break;322	                case TIME:323	                    if (!DateUtils.isTime(fieldValue)) {324	                        return false;325	                    }326	                    try {327	                        Date fieldTime = DateUtils.parseTime(fieldValue);328	                        parsedValue = fieldTime == null ? null : fieldTime.getTime();329	                    } catch (Exception e) {330	                        log.warn("时间格式错误，字段:{},值:{}", schemaField.getSchemaName(), fieldValue);331	                        return false;332	                    }333	                    break;334	                case ARRAY:335	                    try {336	                        if (Keys.TAGS_KEY.equals(key) || Keys.EVENT_IDS_KEY.equals(key)) {337	                            parsedValue = value;338	                        } else {339	                            parsedValue = JSONUtils.parseArray(fieldValue, Object.class);340	                        }341	                    } catch (Exception e) {342	                        log.error("数组字段解析错误：{}", e);343	                        parsedValue = value;344	                    }345	                    break;346	                default:347	                    return false;348	            }349	            row.put(key, parsedValue == null ? fieldValue : parsedValue);350	            return true;351	        } catch (Exception e) {352	            log.error("e: {}", e.getMessage(), e);353	            Loggers.debug("checkDataType and row: \n{} ", () -> JSONUtils.toJSONString(row, true));354	        }355	        return false;356	    }```**StreamApplication.java** — StreamApplication, main, com.haizhi.stream — `/Users/wugao-pc/Desktop/Project/stream/src/main/java/com/haizhi/stream/StreamApplication.java````java1	package com.haizhi.stream;2	3	4	import org.springframework.boot.SpringApplication;5	import org.springframework.boot.autoconfigure.SpringBootApplication;6	import org.springframework.cloud.client.discovery.EnableDiscoveryClient;7	8	@SpringBootApplication9	@EnableDiscoveryClient10	public class StreamApplication {11	12	    public static void main(String[] args) {13	        SpringApplication.run(StreamApplication.class, args);14	    }15	}16	17	```**ValidateType.java** — com.haizhi.stream.validator — `/Users/wugao-pc/Desktop/Project/stream/src/main/java/com/haizhi/stream/validator/ValidateType.java````java1	package com.haizhi.stream.validator;2	3	/**4	 * Create by zhoumingbing on 2019-09-025	 */6	public enum ValidateType {7	    // graph and schema8	    GRAPH_BLANK("graph cannot be blank", RecordLevel.ERROR, "graph为空"),9	    SCHEMA_BLANK("schema cannot be blank", RecordLevel.ERROR, "schema为空"),10	    GRAPH_NOT_FOUND("graph not found:[{0}]", RecordLevel.ERROR, "graph未找到"),11	    SCHEMA_NOT_FOUND("schema not found:[{0}]", RecordLevel.ERROR, "schema未找到"),12	13	    // data rows14	    DATA_ROWS_EMPTY("data rows is empty", RecordLevel.ERROR, "数据行为空"),15	    INVALID_DATA_TYPE_MAIN("invalid main field:[{0}] data type[{1}=>{2}] objectKey=[{3}]", RecordLevel.ERROR,16	            "主标识字段类型错误"),17	    INVALID_DATA_TYPE("invalid data field:[{0}] type[{1}=>{2}] objectKey=[{3}]", RecordLevel.WARNING, "字段类型错误"),18	19	    // schema field20	    MISS_SCHEMA_FIELD("ignore the missing schema filed[{0}]", RecordLevel.WARNING, "传入数据含有多余字段信息"),21	    DELETION_FIELD_VALUE("deletion field -> [{0}] value", RecordLevel.WARNING, "缺失字段(非主标识字段)"),22	    DELETION_MAIN_FIELD_VALUE("deletion main field -> [{0}] value", RecordLevel.ERROR, "缺失字段(主标识字段)"),23	    ILLEGAL_OBJECT_KEY("illegal object key -> [{0}]", RecordLevel.ERROR, "非法object_key"),24	    ILLEGAL_FROM_KEY("illegal from key -> [{0}]", RecordLevel.ERROR, "非法from_key"),25	    ILLEGAL_TO_KEY("illegal to key -> [{0}]", RecordLevel.WARNING, ""),26	    ONLY_OBJECT_KEY("should not only object_key", RecordLevel.ERROR, "仅含有有object_key"),27	    MISS_OBJECT_KEY("missing object_key", RecordLevel.ERROR, "缺失object_key字段"),28	    MISS_FROM_KEY("missing from_key", RecordLevel.ERROR, "缺失from_key字段"),29	    MISS_TO_KEY("missing to_key", RecordLevel.ERROR, "缺失to_key字段"),30	    MISS_FROM_TYPE("missing from_type", RecordLevel.ERROR, "缺失from_type字段"),31	    MISS_TO_TYPE("missing to_type", RecordLevel.ERROR, "缺失to_type字段"),32	    EDGE_MISS_MUST_FIELD("edge must contain object_key,from_key,to_key", RecordLevel.ERROR, "边缺失必要字段"),;33	34	    private String message;35	    private RecordLevel recordType;36	    private String desc;37	38	    ValidateType(String message, RecordLevel recordType, String desc) {39	        this.message = message;40	        this.recordType = recordType;41	        this.desc = desc;42	    }43	44	    public boolean info() {45	        return recordType == RecordLevel.INFO;46	    }47	48	    public boolean warning() {49	        return recordType == RecordLevel.WARNING;50	    }51	52	    public boolean error() {53	        return recordType == RecordLevel.ERROR;54	    }55	56	    public String getMessage() {57	        return message;58	    }59	60	    public RecordLevel getRecordType() {61	        return recordType;62	    }63	64	    public String getDesc() {65	        return desc;66	    }67	68	}69	```"