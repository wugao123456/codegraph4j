package com.codegraph.core.types;

public enum EdgeKind {
    CONTAINS,
    CALLS,
    IMPORTS,
    EXPORTS,
    EXTENDS,
    IMPLEMENTS,
    REFERENCES,
    TYPE_OF,
    RETURNS,
    INSTANTIATES,
    OVERRIDES,
    DECORATES
}